"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
type User={id:number;name:string;username:string;role:string;active:number;created_at:string};
export function UserManager({users}:{users:User[]}){const router=useRouter();const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const res=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const data=await res.json();if(!res.ok)setError(data.error);else{e.currentTarget.reset();router.refresh()}setBusy(false)}async function update(id:number,body:object){const res=await fetch(`/api/admin/users/${id}`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await res.json();if(!res.ok)alert(data.error);router.refresh()}return <div className="settings-grid"><section className="panel"><h2>Equipe editorial</h2><div className="table-wrap"><table className="data-table"><thead><tr><th>Nome</th><th>Perfil</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><strong>{u.name}</strong><small style={{display:"block"}}>@{u.username}</small></td><td>{u.role}</td><td><span className={`status ${u.active?"status-publicado":""}`}>{u.active?"Ativo":"Desativado"}</span></td><td><div className="actions"><button className="btn btn-ghost" onClick={()=>update(u.id,{active:!u.active})}>{u.active?"Desativar":"Ativar"}</button><button className="btn btn-outline" onClick={()=>{const password=prompt("Nova senha (mínimo 10 caracteres):");if(password)update(u.id,{password})}}>Redefinir senha</button></div></td></tr>)}</tbody></table></div></section><form className="panel" onSubmit={create}><h2>Novo usuário</h2>{error&&<p className="form-error">{error}</p>}<div className="field"><label>Nome</label><input className="input" name="name" required/></div><div className="field"><label>Usuário</label><input className="input" name="username" pattern="[a-z0-9._-]{3,32}" required/></div><div className="field"><label>Senha provisória</label><input className="input" type="password" name="password" minLength={10} required/></div><div className="field"><label>Perfil</label><select className="select" name="role"><option value="redator">Redator</option><option value="admin">Administrador</option></select></div><button className="btn btn-primary" disabled={busy}>Criar usuário</button></form></div>}
