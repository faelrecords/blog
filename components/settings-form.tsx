"use client";
import { FormEvent, useState } from "react";
export function SettingsForm({settings}:{settings:Record<string,string>}){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("Salvando...");const values=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/admin/settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)});setMessage(res.ok?"Configurações salvas.":"Não foi possível salvar.")}return <form className="panel" onSubmit={submit}><h2>Configuração do site</h2><div className="field"><label>Título do blog</label><input className="input" name="site_title" defaultValue={settings.site_title}/></div><div className="field"><label>Descrição para buscadores</label><textarea className="textarea" name="site_description" rows={4} defaultValue={settings.site_description}/></div><div className="field"><label>Texto do rodapé</label><input className="input" name="footer_text" defaultValue={settings.footer_text}/></div><h3>Redes sociais</h3><div className="field"><label>LinkedIn</label><input className="input" type="url" name="linkedin_url" defaultValue={settings.linkedin_url}/></div><div className="field"><label>Instagram</label><input className="input" type="url" name="instagram_url" defaultValue={settings.instagram_url}/></div>{message&&<p className="form-success">{message}</p>}<button className="btn btn-primary">Salvar alterações</button></form>}
