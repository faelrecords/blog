import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth";

export default async function LoginPage({searchParams}:{searchParams:Promise<{erro?:string}>}) { const user=await getCurrentUser();if(user)redirect(user.role==="admin"?"/admin":"/publicar");const {erro}=await searchParams;return <main className="auth-page"><section className="auth-art"><div className="brand" style={{color:"white"}}><span className="brand-mark">GT</span>GTChat</div><h1>Conteúdo que transforma conversas.</h1><p>Um espaço simples para criar, revisar e publicar conhecimento com a identidade da marca.</p></section><section className="auth-form-wrap"><form className="auth-card" action="/api/auth/login" method="post"><h2>Bem-vindo</h2><p className="muted">Entre para acessar seu espaço editorial.</p>{erro&&<p className="form-error">Usuário ou senha inválidos.</p>}<div className="field"><label htmlFor="username">Usuário</label><input className="input" id="username" name="username" autoComplete="username" required/></div><div className="field"><label htmlFor="password">Senha</label><input className="input" id="password" name="password" type="password" autoComplete="current-password" required/></div><button className="btn btn-primary" style={{width:"100%"}}>Entrar</button></form></section></main> }
