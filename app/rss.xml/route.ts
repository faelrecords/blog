import { publicPosts, getSettings } from "@/lib/db";
export function GET(){const base=process.env.APP_URL||"http://localhost:3000";const s=getSettings();const items=publicPosts(30).map(p=>`<item><title><![CDATA[${p.title}]]></title><link>${base}/artigos/${p.slug}</link><guid>${base}/artigos/${p.slug}</guid><description><![CDATA[${p.excerpt}]]></description><pubDate>${new Date(p.published_at||Date.now()).toUTCString()}</pubDate></item>`).join("");return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${s.site_title||"GTChat Blog"}</title><link>${base}</link><description>${s.site_description||""}</description>${items}</channel></rss>`,{headers:{"Content-Type":"application/rss+xml; charset=utf-8"}})}
